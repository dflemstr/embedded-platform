[package]
name = "platform-nrf52840"
version = "0.1.0"
authors = ["David Flemström <david.flemstrom@gmail.com>"]
edition = "2018"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
embedded-hal = "0.2.3"
embedded-platform = { path = "../.." }
nrf52840-hal = "0.8.1"

[dev-dependencies]
core = { package = "core-futures-tls", version = "0.1.1" }
cortex-m-rt = "0.6.11"
futures = { version = "0.3.1", default-features = false, features = ["async-await"] }
panic-halt = "0.2.0"
